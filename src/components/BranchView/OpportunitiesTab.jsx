'use client';

import Opportunities from '@/components/Opportunities';

const OpportunitiesTab = ({ branchId }) => {
  return <Opportunities branchId={branchId} />;
};

export default OpportunitiesTab;